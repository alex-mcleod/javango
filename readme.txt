Needed to copy and paste the two files in WebContent/WEB-INF/lib in from Restlet src. 

To make sure that config files are found, need to right-click folder containing files and go to Build Path>Use as Source Folder.